[build]
  publish = "build"
  command = "npm run build"

[[redirects]]
  from = "/api"
  to = "https://newsexplorer-api.herokuapp.com/:splat"
  status = 200
  force = true # COMMENT: ensure that we always redirect
  headers = {X-From = "Netlify"}
